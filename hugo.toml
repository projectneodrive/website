baseURL = "https://projectneodrive.ch"
title   = "Project Neodrive"
languageCode = "en-us"
theme = "hello-friend-ng"

PygmentsCodeFences = true
PygmentsStyle = "monokai"

copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License." # This message is only used by the RSS template.

archetypeDir = "archetypes"
contentDir   = "content"
dataDir      = "data"
layoutDir    = "layouts"
publishDir   = "public"

buildDrafts  = false
buildFuture  = false
buildExpired = false
canonifyURLs = true

enableRobotsTXT = true
enableGitInfo   = false
enableEmoji     = true
enableMissingTranslationPlaceholders = false
disableRSS     = false
disableSitemap = false
disable404     = false
disableHugoGeneratorInject = false

[pagination]
  disableAliases = false
  pagerSize = 20
  path = 'page'
      
#   [services.disqus]
#     shortname = 'your-disqus-shortname'
#   [services.googleAnalytics]
#     ID = 'G-MEASUREMENT_ID'

[permalinks]
  posts = "/posts/:year/:month/:title/"

[blackfriday]
  hrefTargetBlank = true

[taxonomies]
  tag      = "tags"
  category = "categories"
  series   = "series"

[params]
  dateform        = "Jan 2, 2006"
  dateformShort   = "Jan 2"
  dateformNum     = "2006-01-02"
  dateformNumTime = "2006-01-02 15:04"

  # Metadata mostly used in document's head
  # 
  description = "Project Neodrive : Bridging the gap toward sustainable personal mobility"
  keywords = ""
  images = [""]

  # Home subtitle of the index page.
  #
  homeSubtitle = "Project Neodrive : Bridging the gap toward sustainable personal mobility"

  # Set a background for the homepage
  # backgroundImage = "assets/images/background.jpg"

  # Prefix of link to the git commit detail page. GitInfo must be enabled.
  #
  # gitUrl = ""

  # Set disableReadOtherPosts to true in order to hide the links to other posts.
  #
  disableReadOtherPosts = false

  # Enable theme toggle
  # 
  # This options enables the theme toggle for the theme. 
  # Per default, this option is off. 
  # The theme is respecting the prefers-color-scheme of the operating system. 
  # With this option on, the page user is able to set the scheme he wants. 
  enableThemeToggle = true

  # Sharing buttons
  #
  # There are a lot of buttons preconfigured. If you want to change them,
  # generate the buttons here: https://sharingbuttons.io
  # and add them into your own `layouts/partials/sharing-buttons.html`
  #
  enableSharingButtons = true

  # Global language menu
  #
  # Enables the global language menu.
  #
  enableGlobalLanguageMenu = false

  # Integrate Javascript files or stylesheets by adding the url to the external assets or by
  # linking local files with their path relative to the static folder, e.g. "css/styles.css"
  #
  customCSS = []
  customJS  = []

  # Toggle this option need to rebuild SCSS, requires extended version of Hugo
  #
  justifyContent = true  # Set "text-align: justify" to .post-content.

  # Integrate Plausible.io
  # plausibleDataDomain = 'test.com'
  # plausibleScriptSource = 'https://plausible.io/js/script.js'

  # Custom footer 
  # If you want, you can easily override the default footer with your own content. 
  #
  [params.footer]
    rss = false
    copyright = false
    author = false

    topText = []
    bottomText = [
      "Made with &#10084; by <a href=\"https://github.com/projectneodrive\">Project Neodrive Team</a>"
    ]

  # Colors for favicons
  #
  [params.favicon.color]
    mask = "#1b1c1d"
    msapplication = "#1b1c1d"
    theme = "#1b1c1d"

  [params.logo]
    logoMark     = ""
    logoText     = "Project Neodrive"
    logoCursorDisabled = true

  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #  
  # Addon: Startpage portrait 
  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
  # Uncomment this if you want a portrait on your start page
  #
  [params.portrait]
    path     = "img/logo.jpg"
    alt      = "Project Neodrive logo"
    maxWidth = "50px"

  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #  
  # Addon: Social icons
  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
  #
    [[params.social]]
    name = "telegram"
    url  = "https://t.me/projectneodrive"

  [[params.social]]
    name = "github"
    url  = "https://github.com/orgs/projectneodrive"

  [[params.social]]
    name = "instagram"
    url  = "https://www.instagram.com/projectneodrive/"


[menu]

[[menu.main]]
  identifier = "problem"
  name       = "Problem"
  url        = "/problem/"
  weight     = 10

[[menu.main]]
  identifier = "approach"
  name       = "Approach"
  url        = "/approach/"
  weight     = 20

[[menu.main]]
  identifier = "humans"
  name       = "Designing for Humans"
  url        = "/humans/"
  weight     = 30

[[menu.main]]
  identifier = "limits"
  name       = "Living Within Limits"
  url        = "/limits/"
  weight     = 40

[[menu.main]]
  identifier = "physics"
  name       = "The laws of Physics"
  url        = "/physics/"
  weight     = 50

[[menu.main]]
  identifier = "economic"
  name       = "Making It Viable"
  url        = "/economic/"
  weight     = 60

[[menu.main]]
  identifier = "join"
  name       = "Build With Us"
  url        = "/join/"
  weight     = 70
  
[[menu.main]]
  identifier = "vehicles"
  name       = "Vehicles"
  url        = "/vehicles/"
  weight     = 80
